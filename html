<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Portfolio Ultra Interactivo WOW - Tu Nombre</title>
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
<style>
:root {
  --bg-color: #f4f4f4;
  --text-color: #333;
  --accent-color: #ff4c4c;
  --header-bg: #1e1e2f;
  --card-bg: #fff;
}
[data-theme="dark"] {
  --bg-color: #121212;
  --text-color: #e0e0e0;
  --accent-color: #ff6b6b;
  --header-bg: #1a1a2e;
  --card-bg: #1f1f1f;
}
html { scroll-behavior: smooth; }
body { margin:0; padding:0; font-family:'Roboto', sans-serif; background:var(--bg-color); color:var(--text-color); transition:0.3s;}
a{ text-decoration:none; color:inherit; cursor:pointer;}

/* Header */
header{
  position: relative;
  height:100vh;
  display:flex;
  flex-direction:column;
  justify-content:center;
  align-items:center;
  text-align:center;
  overflow:hidden;
}
#particles{position:absolute; top:0; left:0; width:100%; height:100%; z-index:-2;}
header::after{content:""; position:absolute; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.5); z-index:-1;}
header h1, header p, header a.button{opacity:0; transform:translateY(20px);}
header h1.fade-slide, header p.fade-slide, header a.button.fade-slide{animation: fadeSlideIn 1s forwards;}
header h1{animation-delay:0.3s;} header p{animation-delay:0.6s;} header a.button{animation-delay:0.9s;}
header a.button{background-color: var(--accent-color); padding:12px 25px; border-radius:8px; transition:0.3s;}
header a.button:hover, header a.button:active{background:#e04343; transform:scale(1.05); box-shadow:0 0 20px rgba(255,76,76,0.7);}

/* Theme toggle */
#theme-toggle{position:absolute; top:20px; right:20px; z-index:2; background: var(--accent-color); color:white; border:none; padding:10px 15px; border-radius:5px; cursor:pointer; transition:0.3s;}
#theme-toggle:hover{ background:#e04343; transform:scale(1.1); }

/* Sections */
section{padding:60px 20px; max-width:1000px; margin:auto; opacity:0; transform:translateY(30px); transition:0.8s;}
section.visible{opacity:1; transform:translateY(0);}
section h2{text-align:center;margin-bottom:40px;font-size:2rem; color:var(--text-color);}

/* Skills */
#skills{display:flex; justify-content:center; flex-wrap:wrap; gap:12px;}
.skill{background-color:var(--header-bg); color:white; padding:8px 12px; border-radius:5px; transition:0.3s transform 0.5s, box-shadow 0.3s; opacity:0; transform:translateY(20px);}
.skill.visible{opacity:1; transform:translateY(0);}
.skill:hover, .skill:active{background-color:var(--accent-color); transform:scale(1.1); box-shadow:0 5px 15px rgba(255,76,76,0.4); cursor:pointer;}

/* Projects Carousel */
#projects-container{display:flex; overflow-x:auto; scroll-snap-type:x mandatory; gap:15px; padding-bottom:20px;}
#projects-container::-webkit-scrollbar{display:none;}
.project{flex:0 0 80%; max-width:350px; scroll-snap-align:start; background:var(--card-bg); border-radius:12px; overflow:hidden; box-shadow:0 4px 15px rgba(0,0,0,0.1); transition:transform 0.5s, box-shadow 0.5s; opacity:0; transform:translateY(20px);}
.project.visible{opacity:1; transform:translateY(0);}
.project:hover, .project:active{transform:translateY(-8px) rotateY(3deg) scale(1.03); box-shadow:0 12px 25px rgba(255,76,76,0.3);}
.project img{width:100%; height:180px; object-fit:cover;}
.project-content{padding:12px;}
.project-content h3{margin-bottom:8px;}
.project-content p{margin-bottom:8px; font-size:0.9rem; color:var(--text-color);}
.project-content a{margin-right:8px; color:var(--accent-color); font-weight:bold; transition:0.3s;}
.project-content a:hover, .project-content a:active{color:#e04343; transform:scale(1.05);}

/* Testimonials */
#testimonials{display:flex; overflow-x:auto; gap:12px; padding-bottom:20px;}
.testimonial{flex:0 0 80%; background:var(--card-bg); padding:15px; border-radius:12px; box-shadow:0 4px 15px rgba(0,0,0,0.1); transition:transform 0.5s, box-shadow 0.5s; opacity:0; transform:translateY(20px);}
.testimonial.visible{opacity:1; transform:translateY(0);}
.testimonial:hover, .testimonial:active{transform:translateY(-5px) scale(1.05); box-shadow:0 12px 25px rgba(255,76,76,0.3);}
.testimonial p{margin-bottom:8px;color:var(--text-color);}
.testimonial span{font-weight:bold;color:var(--accent-color);}

/* Contact */
#contact form{display:flex; flex-direction:column; max-width:450px; margin:auto; gap:12px;}
#contact input,#contact textarea{padding:10px;border-radius:5px;border:1px solid #ccc;font-size:1rem;width:100%;background:var(--card-bg); color:var(--text-color); transition:0.3s;}
#contact input:focus,#contact textarea:focus{border-color:var(--accent-color); box-shadow:0 0 10px rgba(255,76,76,0.3);}
#contact button{background:var(--header-bg); color:white; border:none; padding:12px; border-radius:5px; cursor:pointer; transition:0.3s;}
#contact button:hover, #contact button:active{background:#33334d; transform:scale(1.05); box-shadow:0 5px 15px rgba(255,76,76,0.3);}
footer{background:var(--header-bg); color:white; text-align:center; padding:20px; font-size:0.9rem;}

/* Animations */
@keyframes fadeSlideIn{to{opacity:1; transform:translateY(0);}}
.typing{display:inline-block;border-right:2px solid var(--accent-color);white-space:nowrap; overflow:hidden;font-weight:bold;}

/* Responsive adjustments */
@media (max-width:768px){
  header h1{font-size:1.8rem;}
  section h2{font-size:1.5rem;}
  .project{flex:0 0 85%;}
  .testimonial{flex:0 0 85%;}
}
</style>
</head>
<body data-theme="light">

<header>
  <canvas id="particles"></canvas>
  <button id="theme-toggle">🌙 Tema</button>
  <h1 class="fade-slide">Elizabeth Jiménez</h1>
  <p class="fade-slide">Soy <span class="typing" id="typing"></span></p>
  <a href="#projects" class="button fade-slide">Ver mis proyectos</a>
</header>

<section id="about">
  <h2>Sobre mí</h2>
  <p>Apasionado por el desarrollo web, creo experiencias interactivas y funcionales usando HTML, CSS, JavaScript y Python. Siempre busco aprender nuevas tecnologías y mejorar la experiencia del usuario.</p>
  <div id="skills">
    <span class="skill">HTML</span>
    <span class="skill">CSS</span>
    <span class="skill">JavaScript</span>
    <span class="skill">Python</span>
  </div>
</section>

<section id="projects">
  <h2>Proyectos Destacados</h2>
  <div id="projects-container">
    <div class="project">
      <img src="https://via.placeholder.com/400x180.png?text=Proyecto+1" alt="Proyecto 1">
      <div class="project-content">
        <h3>Portfolio Personal</h3>
        <p>Portfolio interactivo que muestra mis habilidades y proyectos.</p>
        <a href="#">Ver en vivo</a>
        <a href="#">Código</a>
      </div>
    </div>
    <div class="project">
      <img src="https://via.placeholder.com/400x180.png?text=Proyecto+2" alt="Proyecto 2">
      <div class="project-content">
        <h3>App de Tareas</h3>
        <p>Aplicación para gestionar tareas con JavaScript y Python.</p>
        <a href="#">Ver en vivo</a>
        <a href="#">Código</a>
      </div>
    </div>
  </div>
</section>

<section id="testimonials">
  <h2>Certificaciones / Testimonios</h2>
  <div class="testimonial"><p>Curso de Desarrollo Web Completo</p><span>Platzi</span></div>
  <div class="testimonial"><p>Certificación Python Avanzado</p><span>Coursera</span></div>
</section>

<section id="contact">
  <h2>Contacto</h2>
  <form>
    <input type="text" placeholder="Tu nombre" required>
    <input type="email" placeholder="Tu correo" required>
    <textarea rows="5" placeholder="Tu mensaje" required></textarea>
    <button type="submit">Enviar</button>
  </form>
</section>

<footer>&copy; 2025 Tu Nombre | <a href="#">GitHub</a> | <a href="#">LinkedIn</a></footer>

<script>
// Theme toggle
const toggleBtn=document.getElementById('theme-toggle'); const body=document.body;
toggleBtn.addEventListener('click',()=>{if(body.getAttribute('data-theme')==='light'){body.setAttribute('data-theme','dark'); toggleBtn.textContent='☀️ Tema';}else{body.setAttribute('data-theme','light'); toggleBtn.textContent='🌙 Tema';}});

// Typing effect
const typingText=["Desarrollador Web","Frontend & Backend","Apasionado por la tecnología"];
let index=0,charIndex=0,currentText="",typingElement=document.getElementById('typing');
function type(){if(charIndex<typingText[index].length){currentText+=typingText[index].charAt(charIndex);typingElement.textContent=currentText;charIndex++;setTimeout(type,150);}else{setTimeout(erase,1500);}}
function erase(){if(charIndex>0){currentText=typingText[index].substring(0,charIndex-1);typingElement.textContent=currentText;charIndex--;setTimeout(erase,50);}else{index=(index+1)%typingText.length; setTimeout(type,500);}}
document.addEventListener("DOMContentLoaded",()=>setTimeout(type,500));

// Scroll animations timeline
const sections=document.querySelectorAll('section'); const skills=document.querySelectorAll('.skill'); const projects=document.querySelectorAll('.project'); const testimonials=document.querySelectorAll('.testimonial');
const observer=new IntersectionObserver((entries)=>{
  entries.forEach(entry=>{
    if(entry.isIntersecting){
      if(entry.target.id==='about'){skills.forEach((s,i)=>{setTimeout(()=>s.classList.add('visible'), i*150);});}
      if(entry.target.id==='projects'){projects.forEach((p,i)=>{setTimeout(()=>p.classList.add('visible'), i*200);});}
      if(entry.target.id==='testimonials'){testimonials.forEach((t,i)=>{setTimeout(()=>t.classList.add('visible'), i*200);});}
      entry.target.classList.add('visible');
    }
  });
},{threshold:0.2});
sections.forEach(sec=>observer.observe(sec));

// Particle trail
const canvas=document.getElementById('particles'); const ctx=canvas.getContext('2d'); let width=canvas.width=window.innerWidth,height=canvas.height=window.innerHeight,particles=[];
window.addEventListener('resize',()=>{width=canvas.width=window.innerWidth;height=canvas.height=window.innerHeight;});
class Particle{constructor(x,y){this.x=x; this.y=y; this.size=Math.random()*2+1; this.alpha=1; this.speedX=(Math.random()-0.5)*1; this.speedY=(Math.random()-0.5)*1;}update(){this.x+=this.speedX; this.y+=this.speedY; this.alpha-=0.02;}draw(){ctx.fillStyle='rgba(255,76,76,'+this.alpha+')'; ctx.beginPath(); ctx.arc(this.x,this.y,this.size,0,Math.PI*2); ctx.closePath(); ctx.fill();}}
function animate(){ctx.clearRect(0,0,width,height); particles.forEach((p,i)=>{p.update(); p.draw(); if(p.alpha<=0) particles.splice(i,1);}); requestAnimationFrame(animate);}
canvas.addEventListener('mousemove',e=>{for(let i=0;i<5;i++){particles.push(new Particle(e.x,e.y));}});
animate();
</script>
</body>
</html>
